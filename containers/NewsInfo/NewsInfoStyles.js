import { Text, View, StyleSheet,Button, TouchableOpacity,SafeAreaView,ScrollView } from 'react-native';

export const newsInfoStyles = StyleSheet.create({
  newsInfoContainer:{
    flex: 1,
    padding: 50,
    alignItems: 'center',
    justifyContent: 'center'
  },
  textStyles:{
    fontSize: 20,
    color: 'blue',
    marginBottom: 20
  },

})